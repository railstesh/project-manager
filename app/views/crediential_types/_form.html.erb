<div class = "mt-4 pl-4"> 
  <%= simple_form_for @crediential_type do |form| %>
    <%= form.input :name %>
    <div class="text-right">
      <%= form.submit "Save", class: 'btn btn-success' %>
      <%= link_to 'Back', crediential_types_path, method: :get, class: "btn btn-success"%>
    </div>
  <% end %>
</div>