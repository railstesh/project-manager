<div class = "mt-4 pl-4"> 
  <%= simple_form_for @technology do |form| %>
    <%= form.input :name %>
    <div class="text-right">
      <%= form.submit "Save", class: 'btn btn-success' %>
      <%= link_to 'Back', technologies_path, method: :get, class: "btn btn-success"%>
    </div>
  <% end %>
</div>